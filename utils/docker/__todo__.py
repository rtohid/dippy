__doc__ = """
These are collection of scripts and dockerfiles to automatically build and run dippy on
docker containers. This potentially have two purposes:
    1. ready-to-use, version-controlable dippy environment.
    2. it is handy to have utilities to dynamically build environments especially for
    remote launches.
"""
